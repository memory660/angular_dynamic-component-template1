import { HashDirective } from './hash.directive';

describe('HashDirective', () => {
  it('should create an instance', () => {
    const directive = new HashDirective();
    expect(directive).toBeTruthy();
  });
});
